- [x] currently there are 2 options for the Results panel: either 1 Results webview tab is used for all sas execution (with the tab title changed when the executing SAS script changed), or each new execution create a new webview tab, even when running the same SAS script again. I want an additional config to allow executions of the same SAS script (same editor tab title) reuse the same Results webview tab while executions of a different SAS script will use a different Results webview tab (same title with SAS script editor tab). make sure the previous 2 options are still availble to config
- [x] in data view: key event (Esc, ctrl+a) should only select all cells in data table when the data table is currently focused, otherwise the key event should be passed to the element to handle (such as text selection in the filter expression edit box)
- [x] in data view: allow using mouse to select data. currently, only action is left click to select 1 cell. i want to allow: left click and drag from a cell to select a range, left-clicking a non-selected cell to select it (and cancel any current selection), left-clicking the selected cell/range to cancel all selection (like pressing Esc)
- [x] in data view: change default font size to be same as editor font size config value and add buttons to change font size of data table

